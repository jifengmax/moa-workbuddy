# MoA (Mixture of Agents) — environment template
# Copy to one of the auto-detected locations and fill in your key:
#   - <skill_dir>/.env            (next to the tool)
#   - ~/.workbuddy/.env           (WorkBuddy config dir)
#   - ~/.env                      (home)
# Register for a FREE key at https://opencode.ai/auth
# No quotes, no spaces around '='.
OPENCODE_ZEN_API_KEY=your_free_key_here
